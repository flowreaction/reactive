{
  "name": "reactive",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "start": "bun build ./index.ts --outdir ./out --watch --target browser --sourcemap=inline"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}